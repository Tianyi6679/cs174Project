#include <string>
#include <vector>

cont int d = 21;

struct node{
  bool is_leaf;
  int key_n
  vector<string> key;
  void* children[d+1];
  void* parent;  
  void initial();
  bool insert(string key, vector<int> &values);
  char* search(string key)
};

